
# Text Tabs Admin Panel

Современная админ-панель для управления системой Text Tabs, построенная на React + TypeScript с поддержкой как MySQL, так и Supabase баз данных.

## 🚀 Технологии

- **Frontend**: React 18 + TypeScript + Vite
- **UI Library**: Tailwind CSS + shadcn/ui
- **Backend**: PHP 8+ RESTful API
- **Database**: MySQL 8+ или Supabase PostgreSQL
- **Authentication**: JWT + Row Level Security (RLS)
- **State Management**: React Context + Custom Hooks

## 📋 Возможности

### 🔐 Система авторизации
- JWT аутентификация с fallback на файловые учетные данные
- Защищенные маршруты и проверка токенов
- Автоматическое обновление сессии

### 👥 Управление пользователями
- CRUD операции с пользователями
- Роли: guest, user, premium, admin
- Фильтрация и поиск пользователей
- Статистика активности

### 📄 Управление страницами
- Создание и редактирование HTML страниц пользователей
- Генерация статических файлов
- Система публичных и приватных страниц
- Счетчик просмотров

### 📊 Аналитика и статистика
- Дашборд с ключевыми метриками
- Графики роста пользователей
- Статистика по ролям и активности
- Мониторинг системы

### ⚙️ Настройки системы
- Конфигурация базы данных
- Настройки SMTP для отправки почты
- Управление ролями и ограничениями
- API документация

## 🛠️ Быстрый старт

### 1. Первоначальная настройка

1. **Откройте файл настроек проекта** в корне:
   ```
   http://yourdomain.com/project-setup.html
   ```

2. **Выберите тип базы данных**:
   - MySQL (локальная или хостинговая БД)
   - Supabase (облачная PostgreSQL)

3. **Создайте администратора** нажав кнопку "Создать администратора"

4. **Проверьте подключение** к базе данных

### 2. Настройка базы данных

#### Вариант A: MySQL
1. Создайте базу данных MySQL
2. Обновите настройки в `api/config/database.php`
3. Используйте файл настроек для создания таблиц

#### Вариант B: Supabase
1. Подключите проект к Supabase через интерфейс Lovable
2. Система автоматически создаст необходимые таблицы
3. RLS политики будут настроены автоматически

### 3. Вход в систему

**Учетные данные по умолчанию:**
- Email: `admin@texttabs.com`
- Пароль: `admin123`

## 📁 Структура проекта

```
project/
├── api/                          # PHP Backend API
│   ├── config/                   # Конфигурация
│   │   ├── config.php           # Основные настройки
│   │   ├── database.php         # Настройки БД
│   │   └── admin_credentials.php # Fallback авторизация
│   ├── includes/                 # Вспомогательные файлы
│   │   └── auth.php             # JWT функции
│   ├── auth.php                 # API авторизации
│   ├── users.php               # API пользователей
│   ├── pages.php               # API страниц
│   ├── dashboard.php           # API дашборда
│   ├── settings.php            # API настроек
│   └── project-settings.php    # API настроек проекта
├── src/                         # React Frontend
│   ├── components/              # React компоненты
│   ├── pages/                   # Страницы приложения
│   ├── services/               # API сервисы
│   ├── hooks/                  # Custom hooks
│   └── contexts/               # React контексты
├── project-setup.html          # Файл настройки проекта
└── README.md                   # Документация
```

## 🔧 API Endpoints

### Авторизация
- `POST /api/auth.php` - Вход в систему
- `POST /api/auth.php?action=verify_token` - Проверка токена

### Пользователи
- `GET /api/users.php` - Список пользователей
- `POST /api/users.php` - Создание пользователя
- `PUT /api/users.php` - Обновление пользователя
- `DELETE /api/users.php` - Удаление пользователя

### Страницы
- `GET /api/pages.php` - Список страниц
- `POST /api/pages.php` - Создание страницы
- `PUT /api/pages.php` - Обновление страницы
- `DELETE /api/pages.php` - Удаление страницы

### Статистика
- `GET /api/dashboard.php` - Данные дашборда

### Настройки
- `GET /api/settings.php` - Системные настройки
- `POST /api/settings.php` - Обновление настроек

## 🛡️ Безопасность

### Важные настройки:

1. **Измените секретные ключи** в `api/config/config.php`:
   ```php
   define('JWT_SECRET_KEY', 'your-unique-secret-key');
   define('PASSWORD_SALT', 'your-unique-salt');
   ```

2. **Настройте права доступа к файлам:**
   - PHP файлы: 644
   - Директории: 755
   - Конфигурационные файлы: 600

3. **Обновите учетные данные администратора** после первого входа

## 🌐 Развертывание

### Локальная разработка
```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build
```

### Продакшен
1. Соберите проект: `npm run build`
2. Загрузите содержимое `dist/` в корень хостинга
3. Загрузите папку `api/` в корень хостинга
4. Настройте базу данных через `project-setup.html`
5. Обновите настройки безопасности

## 📊 Мониторинг и отладка

### Инструменты отладки:
- **project-setup.html** - диагностика и настройка
- **Консоль браузера** - логи приложения
- **Network tab** - отслеживание API запросов
- **Логи хостинга** - серверные ошибки

### Типичные проблемы:

1. **Ошибки подключения к БД**
   - Проверьте настройки в `database.php`
   - Убедитесь в правильности учетных данных
   - Проверьте доступность сервера БД

2. **404 ошибки API**
   - Убедитесь, что папка `api/` загружена
   - Проверьте настройки веб-сервера
   - Включите URL Rewrite если необходимо

3. **Проблемы с авторизацией**
   - Проверьте JWT секретный ключ
   - Убедитесь в правильности токенов
   - Проверьте время жизни сессии

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте файл настроек `project-setup.html`
2. Изучите логи в консоли браузера
3. Убедитесь в правильности конфигурации БД
4. Проверьте права доступа к файлам

## 📝 Лицензия

Этот проект создан для внутреннего использования. Все права защищены.

---

**Версия**: 2.0.0  
**Последнее обновление**: Январь 2025
